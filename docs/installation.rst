============
Installation
============

At the command line::

    $ easy_install django-moderation-model-mixin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-moderation-model-mixin
    $ pip install django-moderation-model-mixin
